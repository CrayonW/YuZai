# Sprite Resource Layout

当前 MVP 使用 Canvas 代码绘制占位鱼仔。正式帧图接入时，按动作状态放入对应目录：

- `idle/`
- `walking/`
- `sleeping/`
- `interaction/`
- `placeholder/`

建议文件命名：`state_view_index.png`，例如 `walking_left_0001.png`。
